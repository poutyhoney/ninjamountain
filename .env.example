# Read at request time by apps/web/app/api/triage/route.ts and packages/triage's
# CLI scripts. Not required to lint, typecheck, or build — only needed if you're
# actually exercising those features.

ANTHROPIC_API_KEY=